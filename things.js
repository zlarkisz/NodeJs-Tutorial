let array_counter = function(array) {
  return `Array contain ${array.length} elements`
}

let multiply = function(x, y) {
  return `${x} multiply ${y} equal ${x * y}`
}

let some_value = 451

module.exports = {
  array_counter,
  multiply,
  some_value
}